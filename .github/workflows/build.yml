name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Show project files
        run: ls -la

      - name: CI Success Message
        run: echo "CI/CD успешно работает!"
